id: api
scopes:

queries:
  guides:
    contentType: guide
    scope: list
    orderBy:
      - key: order
        direction: asc

contentTypes:
  include:
    - guide

engine:
  id: json

output:
  path: 'api'
  file: content
  ext: json
